import { UsersNombrePipe } from './users-nombre.pipe';

describe('UsersNombrePipe', () => {
  it('create an instance', () => {
    const pipe = new UsersNombrePipe();
    expect(pipe).toBeTruthy();
  });
});
