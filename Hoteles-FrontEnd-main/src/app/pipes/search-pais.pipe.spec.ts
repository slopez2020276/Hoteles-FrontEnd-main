import { SearchPaisPipe } from './search-pais.pipe';

describe('SearchPaisPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchPaisPipe();
    expect(pipe).toBeTruthy();
  });
});
