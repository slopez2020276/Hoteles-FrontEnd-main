import { SearchPuestoPipe } from './search-puesto.pipe';

describe('SearchPuestoPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchPuestoPipe();
    expect(pipe).toBeTruthy();
  });
});
